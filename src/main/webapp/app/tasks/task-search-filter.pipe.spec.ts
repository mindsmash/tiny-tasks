import { TaskSearchFilterPipe } from './task-search-filter.pipe';

describe('TaskSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
