import { HightlightTextDirective } from './hightlight-text.directive';

describe('HightlightTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightTextDirective();
    expect(directive).toBeTruthy();
  });
});
