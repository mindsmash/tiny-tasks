/**
 * A tiny task.
 */
export interface Task {
  id: string
  name: string
  status: string
}

//here i would prefer using an boolean with: isChecked: true/false - but this attempt worked easier for the beginning
